/**
 * Utility functions
 */

/**
 * Example utility function
 * @param value - The value to process
 * @returns The processed value
 */
export function exampleUtil(value: string): string {
  return value.toUpperCase();
}

